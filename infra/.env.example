# Lightwatch Infrastructure — Environment Variables
# Copy to .env and adjust as needed.
# These values are used in docker-compose.yml.

# MongoDB Atlas connection string
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/monitoring?retryWrites=true&w=majority

# API key for service authentication
API_KEY=

# JWT secret for WebSocket auth (leave empty to disable)
JWT_SECRET=

# Ports (host-side)
INGEST_PORT=3001
REALTIME_PORT=3002
API_PORT=3003
TRAEFIK_HTTP_PORT=80
TRAEFIK_DASHBOARD_PORT=8080
