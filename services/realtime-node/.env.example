# Lightwatch Realtime Service — Environment Variables
# Copy to .env and adjust as needed.

PORT=3002
REDIS_URL=redis://localhost:6379
NODE_ENV=development

# Optional: WS auth token (leave empty to disable)
WS_AUTH_TOKEN=

# Optional: JWT secret for HMAC-SHA256 token verification (leave empty to disable)
JWT_SECRET=

# Optional: plain API key fallback (leave empty to disable)
API_KEY=
