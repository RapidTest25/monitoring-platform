# Lightwatch Ingest Service — Environment Variables
# Copy to .env and adjust as needed.

PORT=3001
MONGO_URI=mongodb://localhost:27017/monitoring
REDIS_URL=redis://localhost:6379
NODE_ENV=development

# Optional: API key for authenticated ingestion (leave empty to disable)
API_KEY=

# Optional: rate limit (requests per window per IP)
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW_MS=60000
